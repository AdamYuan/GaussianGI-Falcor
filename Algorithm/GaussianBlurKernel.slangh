#pragma once

#define GAUSSBLUR_KERNEL_MIN_OFFSET(RADIUS) int(-int(RADIUS))
#define GAUSSBLUR_KERNEL_MAX_OFFSET(RADIUS) int(RADIUS)
#define GAUSSBLUR_KERNEL_WEIGHT_SIZE(RADIUS) ((RADIUS) + 1)
#define GAUSSBLUR_KERNEL_WEIGHT_IDX(OFFSET) (abs(OFFSET))
